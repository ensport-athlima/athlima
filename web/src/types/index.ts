/** Shared types. Content types arrive with their Sanity schemas; keep this file small. */
export type { Phase, PhaseState } from "@/lib/phase"
export type { RouteKey, RoutePath, Pillar, Series } from "@/lib/routes"
