# web/ — read this second

**The constitution is `../CLAUDE.md`. It governs this folder.** When anything here conflicts with it, it wins.

@AGENTS.md is Next.js's own, auto-generated note about its current API surface — `next dev` rewrites it.
Read it for framework facts (the docs in `node_modules/next/dist/docs/` are the current ones), never for
a design, content or dependency decision. Those come from `../06_BUILD/`, `../03_DESIGN_SYSTEM/` and
`../04_CONTENT/`, and from `../08_OPERATIONS/decisions-2026-09-13.md`.
